# TEMPLATE-APP: This is set as a reasonable default, feel free to change.

name: falco
namespace: kube-system
serviceType: managed

project:
  branch: "[[ .Branch ]]"
  commit: "[[ .SHA ]]"

image:
  registry: quay.io
  name: giantswarm/falco
  tag: v0.0.1
  pullPolicy: IfNotPresent
